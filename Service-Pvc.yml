apiversion: v1
kind: persistentVolueClaim
metadata: 
  name: ebs-pvc
spec:
  accessModes:
    - ReadWriteOnce:
  resources:
    requests:
      storage: 1Gi  # Match the size with the PV
   